{
  "name": "lana",
  "displayName": "Log Analyser for Salesforce",
  "version": "1.3.0",
  "description": "Debug Log Profile Analyser",
  "scripts": {},
  "main": "boot.js",
  "icon": "logo.png",
  "publisher": "FinancialForce",
  "author": "",
  "license": "",
  "activationEvents": [
    "onCommand:lana.loadLogFile",
    "onCommand:lana.showLogFile"
  ],
  "engines": {
    "vscode": "^1.41.0"
  },
  "dependencies": {},
  "contributes": {
    "commands": [
      {
        "command": "lana.loadLogFile",
        "title": "Log: Load Log For Analysis"
      },
      {
        "command": "lana.showLogFile",
        "title": "Log: Show Log Analysis"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "lana.showLogFile",
          "when": "resourceExtname == '.log'"
        }
      ],
      "editor/context": [
        {
          "command": "lana.showLogFile",
          "when": "resourceExtname == '.log'"
        }
      ],
      "explorer/context": [
        {
          "command": "lana.showLogFile",
          "when": "resourceExtname == '.log'"
        }
      ]
    }
  }
}
